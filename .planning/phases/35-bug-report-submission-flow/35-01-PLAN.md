---
phase: 35-bug-report-submission-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/actions/bug-reports.ts
  - server/actions/__tests__/bug-reports.test.ts
  - lib/email/bug-report-email.ts
  - lib/email/__tests__/bug-report-email.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [SUB-02, SUB-04, EMAIL-01, EMAIL-02]

must_haves:
  truths:
    - "Bug report is saved to the database with description, pageUrl, submitterEmail, submitterName, browserMetadata, and status='open'"
    - "Admin receives a rich HTML email with submitter info, page URL, description, browser metadata, timestamp, and a dashboard link"
    - "If SES fails, the bug report is still saved and the server action returns success"
    - "User-provided text is HTML-escaped in the email to prevent injection"
  artifacts:
    - path: "server/actions/bug-reports.ts"
      provides: "submitBugReport server action"
      exports: ["submitBugReport"]
    - path: "lib/email/bug-report-email.ts"
      provides: "Email template builder and SES send function"
      exports: ["sendBugReportEmail"]
    - path: "server/actions/__tests__/bug-reports.test.ts"
      provides: "Unit tests for server action"
      min_lines: 50
    - path: "lib/email/__tests__/bug-report-email.test.ts"
      provides: "Unit tests for email template"
      min_lines: 30
  key_links:
    - from: "server/actions/bug-reports.ts"
      to: "lib/email/bug-report-email.ts"
      via: "import sendBugReportEmail"
      pattern: "import.*sendBugReportEmail.*from.*lib/email/bug-report-email"
    - from: "server/actions/bug-reports.ts"
      to: "prisma.bugReport.create"
      via: "Prisma database insert"
      pattern: "db\\.bugReport\\.create"
    - from: "lib/email/bug-report-email.ts"
      to: "@aws-sdk/client-ses"
      via: "SES SendEmailCommand"
      pattern: "SendEmailCommand"
---

<objective>
Install the SES SDK, build the email template with HTML-escaping, and create the server action that saves bug reports to the database and sends fire-and-forget email notifications.

Purpose: Delivers all backend logic for bug report submission (requirements SUB-02, SUB-04, EMAIL-01, EMAIL-02). The server action is the single entry point that Plan 02's UI component will call.
Output: `server/actions/bug-reports.ts`, `lib/email/bug-report-email.ts`, and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-bug-report-submission-flow/35-CONTEXT.md
@.planning/phases/35-bug-report-submission-flow/35-RESEARCH.md
@.planning/phases/35-bug-report-submission-flow/35-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/auth/types.ts:
```typescript
export interface UserInfo {
  sub: string;
  email: string;
  name: string;
  groups: string[];
}
```

From lib/auth/index.ts:
```typescript
export async function getCurrentUser(): Promise<UserInfo>; // redirects to "/" if not authenticated
```

From lib/db.ts:
```typescript
export const db: PrismaClient; // singleton Prisma client, import as `import { db } from "@/lib/db"`
```

From prisma/schema.prisma (BugReport model):
```prisma
model BugReport {
  id               String   @id @default(cuid())
  description      String   @db.Text
  pageUrl          String
  submitterEmail   String
  submitterName    String
  browserMetadata  String   @db.Text
  status           String   @default("open")
  adminNotes       String?  @db.Text
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([status])
  @@index([createdAt])
}
```

From server/actions/shares.ts (reference pattern for server actions):
- Uses `"use server"` directive
- Imports `db` from `@/lib/db`
- Returns `{ success: boolean, error?: string }` shaped objects
- Uses `getAuthorizedProject` for auth (bug-reports will use `getCurrentUser` instead since no project context)

From server/actions/__tests__/shares.test.ts (reference pattern for test mocking):
- Mocks `server-only` with `vi.mock("server-only", () => ({}))`
- Mocks `@/lib/db` with individual model method mocks
- Mocks auth module separately
- Uses `vi.fn()` with `(...args: unknown[])` wrapper pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Email template builder with HTML escaping and SES send</name>
  <files>lib/email/bug-report-email.ts, lib/email/__tests__/bug-report-email.test.ts, package.json, package-lock.json</files>
  <read_first>
    - package.json (check existing @aws-sdk packages for version alignment)
    - server/actions/__tests__/shares.test.ts (mocking patterns)
    - .planning/phases/35-bug-report-submission-flow/35-RESEARCH.md (email template code example)
    - .planning/phases/35-bug-report-submission-flow/35-UI-SPEC.md (email-specific colors section)
  </read_first>
  <behavior>
    - Test: escapeHtml escapes &, <, >, ", ' characters correctly
    - Test: buildEmailHtml output contains submitter name, email, page URL, description, browser viewport, timestamp
    - Test: buildEmailHtml output contains "View in Dashboard" link pointing to /bug-reports
    - Test: buildEmailHtml HTML-escapes user-provided description containing <script> tags
    - Test: buildEmailHtml HTML-escapes submitter name and email containing special chars
    - Test: buildEmailHtml parses browserMetadata JSON to extract viewport dimensions
    - Test: sendBugReportEmail skips silently when BUG_REPORT_ADMIN_EMAIL is undefined
    - Test: sendBugReportEmail skips silently when SES_SENDER_EMAIL is undefined
    - Test: sendBugReportEmail calls SESClient.send with correct Source, Destination, Subject, and HTML body
  </behavior>
  <action>
    1. Run `npm install @aws-sdk/client-ses` to add the SES SDK dependency.

    2. Create `lib/email/__tests__/bug-report-email.test.ts` with tests for:
       - `escapeHtml` function: input `'<script>alert("xss")</script>'` outputs `'&lt;script&gt;alert(&quot;xss&quot;)&lt;/script&gt;'`
       - `buildEmailHtml` function: given a report object `{ description: "Button broken", pageUrl: "/projects/abc", submitterEmail: "user@example.com", submitterName: "Test User", browserMetadata: '{"userAgent":"Mozilla/5.0","viewport":{"width":1920,"height":1080}}', createdAt: new Date("2026-03-26T12:00:00Z") }`, the returned HTML string:
         - Contains `Test User`
         - Contains `user@example.com`
         - Contains `/projects/abc`
         - Contains `Button broken`
         - Contains `1920x1080`
         - Contains `2026-03-26T12:00:00.000Z`
         - Contains `href=` with `/bug-reports`
         - Contains `View in Dashboard`
       - `buildEmailHtml` with XSS input: description `'<img src=x onerror=alert(1)>'` should NOT contain `<img` in output
       - `sendBugReportEmail`: mock SESClient, verify it calls `send` with a `SendEmailCommand` containing Source matching `process.env.SES_SENDER_EMAIL` and Destination.ToAddresses containing `process.env.BUG_REPORT_ADMIN_EMAIL`
       - `sendBugReportEmail` when env vars missing: should NOT call SESClient.send, should return without error
       Mock `@aws-sdk/client-ses` module: `vi.mock("@aws-sdk/client-ses", () => ({ SESClient: vi.fn().mockImplementation(() => ({ send: mockSend })), SendEmailCommand: vi.fn() }))`
       Set env vars in beforeEach: `process.env.BUG_REPORT_ADMIN_EMAIL = "admin@example.com"; process.env.SES_SENDER_EMAIL = "noreply@example.com"; process.env.NEXT_PUBLIC_APP_URL = "https://app.example.com"`
       Clean env vars in afterEach.

    3. Create `lib/email/bug-report-email.ts` with:
       - `escapeHtml(str: string): string` — exported, replaces `&` -> `&amp;`, `<` -> `&lt;`, `>` -> `&gt;`, `"` -> `&quot;`, `'` -> `&#039;`
       - `buildEmailHtml(report: BugReportEmailData): string` — exported, builds the full HTML email. Use inline CSS only (no external stylesheets). Colors per UI-SPEC: header banner `#dc2626`, body bg `#f4f4f5`, card bg `#ffffff`, card border `#e4e4e7`, description block bg `#f4f4f5` with 6px border-radius, CTA button `#18181b` bg with `#ffffff` text. Font: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`. Subject line: `Bug Report: {pageUrl}`. Max-width 560px card centered. HTML-escape ALL user-provided fields before insertion.
       - `sendBugReportEmail(report: BugReportEmailData): Promise<void>` — exported. Reads `BUG_REPORT_ADMIN_EMAIL` and `SES_SENDER_EMAIL` from `process.env`. If either is missing, log `console.warn("[BugReport] Email config missing, skipping notification")` and return. Instantiate SESClient at module level with `region: process.env.AWS_REGION || "us-east-1"`. Build `SendEmailCommand` with Source, Destination.ToAddresses, Subject `{ Data: "Bug Report: {pageUrl}", Charset: "UTF-8" }`, Body.Html `{ Data: buildEmailHtml(report), Charset: "UTF-8" }`. Await `ses.send(command)`.
       - Type: `export interface BugReportEmailData { description: string; pageUrl: string; submitterEmail: string; submitterName: string; browserMetadata: string; createdAt: Date; }`
       - Dashboard link: `${process.env.NEXT_PUBLIC_APP_URL || ""}/bug-reports`

    4. Run tests: `npx vitest run lib/email/__tests__/bug-report-email.test.ts`
  </action>
  <verify>
    <automated>npx vitest run lib/email/__tests__/bug-report-email.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/email/bug-report-email.ts contains `export function escapeHtml(`
    - lib/email/bug-report-email.ts contains `export function buildEmailHtml(`
    - lib/email/bug-report-email.ts contains `export async function sendBugReportEmail(`
    - lib/email/bug-report-email.ts contains `export interface BugReportEmailData`
    - lib/email/bug-report-email.ts contains `SendEmailCommand`
    - lib/email/bug-report-email.ts contains `#dc2626` (email header color)
    - lib/email/bug-report-email.ts contains `/bug-reports` (dashboard link)
    - lib/email/bug-report-email.ts contains `escapeHtml(report.description)`
    - lib/email/__tests__/bug-report-email.test.ts exits 0
    - package.json contains `@aws-sdk/client-ses`
  </acceptance_criteria>
  <done>Email template generates rich HTML with all required fields, HTML-escapes user input, and SES send works with proper env var guards. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: submitBugReport server action with Prisma save and fire-and-forget email</name>
  <files>server/actions/bug-reports.ts, server/actions/__tests__/bug-reports.test.ts</files>
  <read_first>
    - server/actions/shares.ts (reference server action pattern)
    - server/actions/__tests__/shares.test.ts (reference test mocking pattern)
    - lib/auth/index.ts (getCurrentUser import)
    - lib/email/bug-report-email.ts (sendBugReportEmail import - created in Task 1)
  </read_first>
  <behavior>
    - Test: submitBugReport with valid input creates a BugReport record with description, pageUrl, submitterEmail from session user, submitterName from session user, browserMetadata, status "open"
    - Test: submitBugReport returns { success: true } on successful save
    - Test: submitBugReport calls sendBugReportEmail after saving to DB
    - Test: submitBugReport returns { success: true } even when sendBugReportEmail throws (fire-and-forget per D-10)
    - Test: submitBugReport logs error when email fails with console.error containing "[BugReport]"
    - Test: submitBugReport with description < 10 chars returns { success: false, error: "Description must be at least 10 characters" }
    - Test: submitBugReport with empty description returns { success: false }
    - Test: submitBugReport calls getCurrentUser (auth check)
  </behavior>
  <action>
    1. Create `server/actions/__tests__/bug-reports.test.ts`:
       - Mock `server-only`: `vi.mock("server-only", () => ({}))`
       - Mock `@/lib/auth` with `getCurrentUser` returning `{ sub: "user-123", email: "user@example.com", name: "Test User", groups: ["users"] }`
       - Mock `@/lib/db` with `db.bugReport.create` returning a report object with `id: "report-1"`, `createdAt: new Date()`, plus the input fields
       - Mock `@/lib/email/bug-report-email` with `sendBugReportEmail: vi.fn().mockResolvedValue(undefined)`
       - Valid input fixture: `{ description: "The button is broken on the projects page", pageUrl: "/projects/abc123", browserMetadata: '{"userAgent":"Mozilla/5.0","viewport":{"width":1920,"height":1080}}' }`
       - Test "saves report to database": call submitBugReport with valid input, assert `mockBugReportCreate` called with `{ data: { description: "The button is broken on the projects page", pageUrl: "/projects/abc123", submitterEmail: "user@example.com", submitterName: "Test User", browserMetadata: expect.any(String), status: "open" } }`
       - Test "returns success": assert result is `{ success: true }`
       - Test "sends email after save": assert `mockSendBugReportEmail` called once with object containing `description`, `submitterEmail`
       - Test "returns success when email fails": make `mockSendBugReportEmail.mockRejectedValueOnce(new Error("SES error"))`, call submitBugReport, assert result is `{ success: true }`
       - Test "logs error when email fails": spy on `console.error`, make email mock reject, assert `console.error` called with string containing `[BugReport]`
       - Test "rejects short description": call with `{ description: "short", pageUrl: "/", browserMetadata: "{}" }`, assert result is `{ success: false, error: "Description must be at least 10 characters" }`
       - Test "rejects empty description": call with `{ description: "", ... }`, assert `{ success: false }`

    2. Create `server/actions/bug-reports.ts`:
       ```
       "use server";

       import { db } from "@/lib/db";
       import { getCurrentUser } from "@/lib/auth";
       import { sendBugReportEmail } from "@/lib/email/bug-report-email";

       export async function submitBugReport(data: {
         description: string;
         pageUrl: string;
         browserMetadata: string;
       }) {
         const user = await getCurrentUser();

         if (!data.description || data.description.trim().length < 10) {
           return { success: false as const, error: "Description must be at least 10 characters" };
         }

         const report = await db.bugReport.create({
           data: {
             description: data.description.trim(),
             pageUrl: data.pageUrl,
             submitterEmail: user.email,
             submitterName: user.name,
             browserMetadata: data.browserMetadata,
             status: "open",
           },
         });

         try {
           await sendBugReportEmail(report);
         } catch (error) {
           console.error("[BugReport] Email notification failed:", error);
         }

         return { success: true as const };
       }
       ```

    3. Run tests: `npx vitest run server/actions/__tests__/bug-reports.test.ts`
  </action>
  <verify>
    <automated>npx vitest run server/actions/__tests__/bug-reports.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/actions/bug-reports.ts starts with `"use server"`
    - server/actions/bug-reports.ts contains `export async function submitBugReport(`
    - server/actions/bug-reports.ts contains `getCurrentUser()`
    - server/actions/bug-reports.ts contains `db.bugReport.create(`
    - server/actions/bug-reports.ts contains `sendBugReportEmail(report)`
    - server/actions/bug-reports.ts contains `console.error("[BugReport] Email notification failed:"`
    - server/actions/bug-reports.ts contains `status: "open"`
    - server/actions/bug-reports.ts contains `success: true`
    - server/actions/bug-reports.ts contains `Description must be at least 10 characters`
    - server/actions/__tests__/bug-reports.test.ts exits 0
  </acceptance_criteria>
  <done>Server action saves bug reports to DB, sends fire-and-forget email, validates description length, and all tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run lib/email/__tests__/bug-report-email.test.ts` -- all email template tests pass
2. `npx vitest run server/actions/__tests__/bug-reports.test.ts` -- all server action tests pass
3. `npx vitest run` -- full suite still green (no regressions)
4. `grep -r "@aws-sdk/client-ses" package.json` -- dependency installed
</verification>

<success_criteria>
- submitBugReport server action exists and is importable
- Email template generates rich HTML with all D-07/D-09 fields
- HTML escaping prevents injection in email content
- Fire-and-forget pattern: email failure does not affect action result (D-10)
- Missing env vars handled gracefully with console.warn
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/35-bug-report-submission-flow/35-01-SUMMARY.md`
</output>
