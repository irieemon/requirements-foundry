---
phase: 21-application-code-migration
plan: 05
type: execute
wave: 1
depends_on: ["21-03", "21-04"]
files_modified:
  - server/actions/generation.ts
  - lib/batch-stories/__tests__/generate-all-stories.test.ts
  - prisma/schema.prisma
autonomous: true
gap_closure: true
requirements: [CODE-07]

must_haves:
  truths:
    - "server/actions/generation.ts compiles without TypeScript errors"
    - "server/actions/generation.ts uses async hasAwsCredentials() instead of removed hasAnthropicKey()"
    - "All test files mock hasAwsCredentials instead of hasAnthropicKey"
    - "No references to hasAnthropicKey remain in the codebase"
    - "prisma/schema.prisma has no Vercel references in comments"
  artifacts:
    - path: "server/actions/generation.ts"
      provides: "Epic and story generation server actions with AWS credential detection"
      contains: "hasAwsCredentials"
    - path: "lib/batch-stories/__tests__/generate-all-stories.test.ts"
      provides: "Batch story generation tests with correct mocks"
      contains: "hasAwsCredentials"
    - path: "prisma/schema.prisma"
      provides: "Database schema without Vercel references"
  key_links:
    - from: "server/actions/generation.ts"
      to: "lib/ai/provider.ts"
      via: "import { getAIProvider, hasAwsCredentials }"
      pattern: "import.*hasAwsCredentials.*from.*@/lib/ai/provider"
---

<objective>
Fix three verification gaps from Phase 21 execution: (1) broken hasAnthropicKey import in generation.ts, (2) stale mock in test file, (3) cosmetic Vercel comment in schema.

Purpose: Close build-blocking TypeScript compilation error and remaining Vercel references found during verification.
Output: All three files patched, TypeScript compiles, no hasAnthropicKey references remain.
</objective>

<execution_context>
@/Users/smcine01/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smcine01/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-application-code-migration/21-VERIFICATION.md

<interfaces>
<!-- From lib/ai/provider.ts (modified in Plan 03): -->

```typescript
// The hasAnthropicKey export was REMOVED. These are the current exports:
export async function hasAwsCredentials(): Promise<boolean>;  // cached, returns boolean
export async function getAIProvider(): Promise<AIProvider>;   // now async, returns Promise
```

<!-- Key difference: hasAwsCredentials is async (returns Promise<boolean>), hasAnthropicKey was sync (returned boolean). All call sites must use await. -->
<!-- getAIProvider is now async (returns Promise<AIProvider>). All call sites must use await. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hasAnthropicKey references in generation.ts and test file</name>
  <files>server/actions/generation.ts, lib/batch-stories/__tests__/generate-all-stories.test.ts</files>
  <action>
In `server/actions/generation.ts`:

1. Line 5: Change import from `{ getAIProvider, hasAnthropicKey }` to `{ getAIProvider, hasAwsCredentials }` from `@/lib/ai/provider`

2. Line 66: Change `const provider = getAIProvider();` to `const provider = await getAIProvider();` (getAIProvider is now async per Plan 03)

3. Line 67: Change `const isRealAI = hasAnthropicKey();` to `const isRealAI = await hasAwsCredentials();`

4. Line 69: Change log message from `Using ${isRealAI ? "Anthropic API" : "Mock Provider"}` to `Using ${isRealAI ? "Bedrock AI" : "Mock Provider"}`

5. Line 202: Change `const provider = getAIProvider();` to `const provider = await getAIProvider();`

6. Line 203: Change `const isRealAI = hasAnthropicKey();` to `const isRealAI = await hasAwsCredentials();`

7. Line 206: Change log message from `Using ${isRealAI ? "Anthropic API" : "Mock Provider"}` to `Using ${isRealAI ? "Bedrock AI" : "Mock Provider"}`

In `lib/batch-stories/__tests__/generate-all-stories.test.ts`:

1. Line 19: Change mock from `hasAnthropicKey: vi.fn()` to `hasAwsCredentials: vi.fn()` in the vi.mock block for `@/lib/ai/provider`
  </action>
  <verify>
    <automated>cd "/Users/smcine01/Library/CloudStorage/OneDrive-dentsu/Documents/Merkle/Claude Projects/Requirements Foundy" && grep -rn "hasAnthropicKey" server/ lib/ app/ components/ --include="*.ts" --include="*.tsx" && echo "FAIL: hasAnthropicKey still found" || echo "PASS: no hasAnthropicKey references"</automated>
  </verify>
  <done>
    - server/actions/generation.ts imports hasAwsCredentials (not hasAnthropicKey)
    - Both getAIProvider() calls use await
    - Both hasAwsCredentials() calls use await
    - Log messages say "Bedrock AI" not "Anthropic API"
    - Test file mocks hasAwsCredentials instead of hasAnthropicKey
    - Zero grep hits for hasAnthropicKey in codebase
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Vercel comment from prisma/schema.prisma</name>
  <files>prisma/schema.prisma</files>
  <action>
Line 2 of `prisma/schema.prisma` currently reads:
```
// PostgreSQL for production (Vercel), SQLite-compatible for local dev via env
```

Change to:
```
// PostgreSQL for production, SQLite-compatible for local dev via env
```

Simply remove the "(Vercel)" parenthetical. Keep the rest of the comment intact.
  </action>
  <verify>
    <automated>cd "/Users/smcine01/Library/CloudStorage/OneDrive-dentsu/Documents/Merkle/Claude Projects/Requirements Foundy" && grep -in "vercel" prisma/schema.prisma && echo "FAIL: Vercel reference found" || echo "PASS: no Vercel references in schema"</automated>
  </verify>
  <done>
    - prisma/schema.prisma line 2 has no Vercel reference
    - Comment still describes PostgreSQL for production
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "hasAnthropicKey" --include="*.ts" --include="*.tsx" server/ lib/ app/ components/` returns empty
2. `grep -in "vercel" prisma/schema.prisma` returns empty
3. `npx tsc --noEmit 2>&1 | head -20` shows no errors related to hasAnthropicKey or generation.ts (full type check may have other pre-existing issues)
</verification>

<success_criteria>
- Zero references to hasAnthropicKey in codebase
- server/actions/generation.ts uses await getAIProvider() and await hasAwsCredentials()
- Test mock updated to hasAwsCredentials
- prisma/schema.prisma has no Vercel comment
- CODE-07 gap fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/21-application-code-migration/21-05-SUMMARY.md`
</output>
