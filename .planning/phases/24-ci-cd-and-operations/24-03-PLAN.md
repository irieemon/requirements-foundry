---
phase: 24-ci-cd-and-operations
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
  - 24-02
files_modified:
  - infra/lib/requirements-foundry-stack.ts
autonomous: false
requirements:
  - CICD-01
  - CICD-02
  - CRON-01
  - OPS-02
  - OPS-03

user_setup:
  - service: github
    why: "OIDC authentication requires AWS_ACCOUNT_ID in GitHub secrets"
    env_vars:
      - name: AWS_ACCOUNT_ID
        source: "Run: aws sts get-caller-identity --query Account --output text"
    dashboard_config:
      - task: "Add repository secret AWS_ACCOUNT_ID"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"
  - service: aws-sns
    why: "SNS email subscription requires manual confirmation"
    dashboard_config:
      - task: "Confirm SNS subscription email"
        location: "Check inbox for AWS SNS subscription confirmation email after cdk deploy"

must_haves:
  truths:
    - "CDK deploys successfully with all Phase 24 constructs"
    - "EventBridge rule is active and Lambda invocations appear in CloudWatch Logs"
    - "CloudWatch alarms exist and are in OK state (service is healthy)"
    - "SNS email subscription is confirmed"
  artifacts:
    - path: "infra/lib/requirements-foundry-stack.ts"
      provides: "Deployed CDK stack with all Phase 24 infrastructure"
  key_links:
    - from: "CDK stack"
      to: "AWS CloudFormation"
      via: "cdk deploy"
      pattern: "RequirementsFoundryStack"
---

<objective>
Deploy the Phase 24 CDK infrastructure to AWS and verify all constructs are working: OIDC provider, Lambda cron caller, EventBridge schedule, CloudWatch alarms, and SNS notifications.

Purpose: Infrastructure must be live before the GitHub Actions workflow can run. This plan deploys and validates everything end-to-end.
Output: Live AWS infrastructure supporting automated deployments, scheduled cron, and operational monitoring.
</objective>

<execution_context>
@/Users/smcine01/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smcine01/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ci-cd-and-operations/24-CONTEXT.md
@.planning/phases/24-ci-cd-and-operations/24-RESEARCH.md
@.planning/phases/24-ci-cd-and-operations/24-01-SUMMARY.md
@.planning/phases/24-ci-cd-and-operations/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy CDK stack with Phase 24 infrastructure</name>
  <files>infra/lib/requirements-foundry-stack.ts</files>
  <action>
Run CDK diff first to preview changes:
```bash
cd infra && npx cdk diff
```

Review the diff output. Expected new resources:
- AWS::IAM::OIDCProvider (GitHub OIDC)
- AWS::IAM::Role (GitHub Actions role)
- AWS::SecretsManager::Secret (CRON_SECRET)
- AWS::Lambda::Function (cron caller)
- AWS::Events::Rule (5-min schedule)
- AWS::SNS::Topic (alarm notifications)
- AWS::CloudWatch::Alarm (x3)
- Modified: AWS::ECS::Service (deployment config)
- Modified: AWS::ECS::TaskDefinition (CRON_SECRET env var)

If the OIDC provider creation fails with "Provider with url already exists", update the CDK stack to use `iam.OpenIdConnectProvider.fromOpenIdConnectProviderArn()` with the existing provider's ARN instead of creating a new one. Find the existing ARN with:
```bash
aws iam list-open-id-connect-providers --query 'OpenIDConnectProviderList[?contains(Arn, `token.actions.githubusercontent.com`)]'
```

Deploy with alarm email context parameter:
```bash
cd infra && npx cdk deploy --context alarmEmail=<user-provided-email> --require-approval never
```

Note: If the user has not provided an email, deploy without the context parameter (SNS topic will be created but without a subscription -- can be added later).

After successful deploy, verify key outputs:
```bash
aws cloudformation describe-stacks --stack-name RequirementsFoundryStack --query 'Stacks[0].Outputs' --no-cli-pager
```
  </action>
  <verify>
    <automated>aws cloudformation describe-stacks --stack-name RequirementsFoundryStack --query 'Stacks[0].StackStatus' --output text --region us-east-1</automated>
  </verify>
  <done>CDK deploy completes. Stack status is UPDATE_COMPLETE. New outputs include GitHubActionsRoleArn, CronSecretArn, and AlarmTopicArn.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployment, confirm SNS subscription, set GitHub secret</name>
  <files>infra/lib/requirements-foundry-stack.ts</files>
  <action>
Phase 24 CDK infrastructure has been deployed. The user needs to verify the following and complete manual setup steps.

What was built:
- OIDC provider and GitHub Actions IAM role for CI/CD
- CRON_SECRET in Secrets Manager, Lambda cron caller, EventBridge 5-min schedule
- 3 CloudWatch alarms (ECS tasks=0, ALB unhealthy, RDS CPU>80%)
- SNS alarm topic with email subscription
- GitHub Actions deploy workflow at .github/workflows/deploy.yml

Verification steps for the user:

1. SNS Subscription: Check email inbox for AWS SNS subscription confirmation. Click "Confirm subscription" link. Verify with:
   aws sns list-subscriptions-by-topic --topic-arn $(aws cloudformation describe-stacks --stack-name RequirementsFoundryStack --query 'Stacks[0].Outputs[?ExportName==`rf-prod-alarm-topic-arn`].OutputValue' --output text --region us-east-1) --region us-east-1

2. EventBridge + Lambda: Check cron schedule is active:
   aws events describe-rule --name requirements-foundry-cron-schedule --region us-east-1
   Check Lambda logs (may take up to 5 minutes for first invocation):
   aws logs tail /aws/lambda/requirements-foundry-cron-caller --region us-east-1 --since 10m

3. CloudWatch Alarms: Verify all 3 alarms exist:
   aws cloudwatch describe-alarms --alarm-name-prefix rf-prod --query 'MetricAlarms[].{Name:AlarmName,State:StateValue}' --region us-east-1

4. GitHub Setup: Add AWS_ACCOUNT_ID as a repository secret:
   Go to https://github.com/irieemon/requirements-foundry/settings/secrets/actions
   Name: AWS_ACCOUNT_ID, Value: (run aws sts get-caller-identity --query Account --output text)

5. Optional: Push a small commit to main and verify GitHub Actions workflow runs at:
   https://github.com/irieemon/requirements-foundry/actions

Resume signal: Type "approved" when SNS subscription is confirmed, GitHub secret is set, and Lambda is running. Or describe any issues.
  </action>
  <verify>aws cloudwatch describe-alarms --alarm-name-prefix rf-prod --query 'MetricAlarms[].AlarmName' --output text --region us-east-1</verify>
  <done>SNS subscription confirmed, GitHub AWS_ACCOUNT_ID secret set, EventBridge rule enabled, all 3 CloudWatch alarms active.</done>
</task>

</tasks>

<verification>
- Stack deployed with all Phase 24 resources
- EventBridge rule is ENABLED and Lambda has invocation logs
- All 3 CloudWatch alarms exist (rf-prod-ecs-no-running-tasks, rf-prod-alb-unhealthy-targets, rf-prod-rds-high-cpu)
- SNS email subscription is confirmed
- GitHub AWS_ACCOUNT_ID secret is set
- (Optional) GitHub Actions deploy workflow executes successfully on push to main
</verification>

<success_criteria>
- CDK deploy succeeds with all Phase 24 constructs
- Lambda cron caller invokes successfully (visible in CloudWatch Logs)
- CloudWatch alarms are in OK state
- SNS subscription confirmed (email notifications will work)
- GitHub Actions workflow is ready to run (OIDC role deployed, secret configured)
</success_criteria>

<output>
After completion, create `.planning/phases/24-ci-cd-and-operations/24-03-SUMMARY.md`
</output>
