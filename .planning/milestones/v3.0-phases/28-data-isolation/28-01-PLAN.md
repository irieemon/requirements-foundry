---
phase: 28-data-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth/authorization.ts
  - lib/auth/__tests__/authorization.test.ts
  - prisma/schema.prisma
  - prisma/migrations/*_add_user_ownership/migration.sql
autonomous: true
requirements: [DATA-04, ADMIN-01, ADMIN-03]

must_haves:
  truths:
    - "isAdmin('sean.mcinerney@merkle.com') returns true"
    - "isAdmin('other@example.com') returns false"
    - "getAuthorizedProject returns project when user is owner"
    - "getAuthorizedProject calls notFound() when user is not owner and not admin"
    - "getAuthorizedProject returns project when user is admin even if not owner"
    - "getAuthorizedProjects returns only user's projects for non-admin"
    - "getAuthorizedProjects returns all projects for admin"
    - "Existing projects have userId backfilled to admin email"
    - "Project.userId is non-nullable after migration"
  artifacts:
    - path: "lib/auth/authorization.ts"
      provides: "Centralized authorization helpers"
      exports: ["ADMIN_EMAIL", "isAdmin", "getAuthorizedProject", "getAuthorizedProjects"]
    - path: "lib/auth/__tests__/authorization.test.ts"
      provides: "Unit tests for authorization module"
      min_lines: 50
    - path: "prisma/schema.prisma"
      provides: "Updated Project model with required userId"
      contains: "userId      String"
  key_links:
    - from: "lib/auth/authorization.ts"
      to: "lib/auth/index.ts"
      via: "import getCurrentUser"
      pattern: "getCurrentUser"
    - from: "lib/auth/authorization.ts"
      to: "lib/db"
      via: "import db for Prisma queries"
      pattern: "db\\.project\\."
---

<objective>
Create the centralized authorization module and Prisma migration that form the foundation for all data isolation.

Purpose: Every subsequent ownership check depends on these helpers and the non-nullable userId column. This plan establishes the contracts that Plans 02 and 03 implement against.
Output: `lib/auth/authorization.ts` with isAdmin/getAuthorizedProject/getAuthorizedProjects, unit tests, and a Prisma migration that backfills existing projects and makes userId required.
</objective>

<execution_context>
@/Users/smcine01/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smcine01/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-data-isolation/28-CONTEXT.md
@.planning/phases/28-data-isolation/28-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/auth/index.ts:
```typescript
import "server-only";
export type { UserInfo } from "./types";
export { getSession } from "./session";
export async function getCurrentUser(): Promise<UserInfo>;
// Redirects to "/" if not authenticated
```

From lib/auth/types.ts:
```typescript
export interface UserInfo {
  sub: string;
  email: string;
  name: string;
  groups: string[];
}
```

From prisma/schema.prisma (current):
```prisma
model Project {
  id          String   @id @default(cuid())
  name        String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  userId      String?  // Currently nullable -- this plan makes it required

  uploads Upload[]
  cards   Card[]
  epics   Epic[]
  runs    Run[]

  @@index([userId])
  @@index([createdAt])
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create authorization module with TDD tests</name>
  <files>lib/auth/authorization.ts, lib/auth/__tests__/authorization.test.ts</files>
  <behavior>
    - isAdmin("sean.mcinerney@merkle.com") returns true
    - isAdmin("other@example.com") returns false
    - isAdmin("") returns false
    - ADMIN_EMAIL equals "sean.mcinerney@merkle.com"
    - getAuthorizedProject returns { project, user, isAdmin } when user owns the project
    - getAuthorizedProject returns { project, user, isAdmin: true } when admin accesses any project
    - getAuthorizedProject calls notFound() when project does not exist
    - getAuthorizedProject calls notFound() when user is not owner and not admin
    - getAuthorizedProjects returns only the user's projects (filtered by userId) for non-admin
    - getAuthorizedProjects returns all projects for admin (no where filter)
    - getAuthorizedProjects returns { projects, user, isAdmin } shape
  </behavior>
  <action>
    Create `lib/auth/__tests__/authorization.test.ts` first (RED phase):
    - Mock `getCurrentUser` from `@/lib/auth` using vi.mock -- return a configurable UserInfo
    - Mock `db` from `@/lib/db` using vi.mock -- mock `db.project.findUnique` and `db.project.findMany`
    - Mock `notFound` from `next/navigation` using vi.mock
    - Write tests for all behaviors listed above
    - Run tests -- they should FAIL (module doesn't exist yet)

    Then create `lib/auth/authorization.ts` (GREEN phase):
    - Import "server-only" at top
    - Import `notFound` from "next/navigation"
    - Import `getCurrentUser` from "./index"
    - Import `db` from "@/lib/db"
    - Export `ADMIN_EMAIL = "sean.mcinerney@merkle.com"` (hardcoded per user decision -- Okta group detection deferred)
    - Export `isAdmin(email: string): boolean` -- simple email comparison
    - Export `async getAuthorizedProject(projectId: string)` -- fetches project, checks ownership or admin, calls notFound() if denied, returns { project, user, isAdmin }
    - Export `async getAuthorizedProjects()` -- fetches all projects for admin, user's projects for non-admin, returns { projects, user, isAdmin }
    - For getAuthorizedProjects, include the same `orderBy` and `_count` include as the current `getProjects()` in server/actions/projects.ts

    Note: The "server-only" import will cause test failures if not mocked. Add `vi.mock("server-only", () => ({}))` in the test file.
  </action>
  <verify>
    <automated>cd "/Users/smcine01/Library/CloudStorage/OneDrive-dentsu/Documents/Merkle/Claude Projects/Requirements Foundy" && npx vitest run lib/auth/__tests__/authorization.test.ts --reporter=verbose</automated>
  </verify>
  <done>All authorization tests pass. ADMIN_EMAIL, isAdmin, getAuthorizedProject, and getAuthorizedProjects are exported and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Prisma migration and schema update for required userId</name>
  <files>prisma/schema.prisma, prisma/migrations/*_add_user_ownership/migration.sql</files>
  <action>
    Step 1: Create a manual Prisma migration directory and SQL file.
    Run: `npx prisma migrate dev --create-only --name add_user_ownership`
    This creates the migration directory without applying it.

    Step 2: Edit the generated migration.sql to contain exactly:
    ```sql
    -- Backfill existing projects with admin email
    UPDATE "Project" SET "userId" = 'sean.mcinerney@merkle.com' WHERE "userId" IS NULL;

    -- Make userId non-nullable
    ALTER TABLE "Project" ALTER COLUMN "userId" SET NOT NULL;
    ```
    CRITICAL: Backfill MUST come before ALTER COLUMN. If reversed, the migration will fail with "column contains null values".

    Step 3: Update `prisma/schema.prisma` -- change `userId String?` to `userId String` (remove the `?`). Remove the comment "// Nullable for MVP; add auth later". Keep the @@index([userId]).

    Step 4: Run `npx prisma generate` to regenerate the Prisma client with the updated schema (non-nullable userId).

    Do NOT run `npx prisma migrate deploy` -- the migration will apply automatically on the next deploy via entrypoint.js. For local verification, the executor can optionally run `npx prisma migrate dev` if a local database is available.
  </action>
  <verify>
    <automated>cd "/Users/smcine01/Library/CloudStorage/OneDrive-dentsu/Documents/Merkle/Claude Projects/Requirements Foundy" && npx prisma validate && npx prisma generate</automated>
  </verify>
  <done>prisma/schema.prisma has `userId String` (non-nullable). Migration SQL exists with backfill-then-alter pattern. Prisma client regenerated.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/auth/__tests__/authorization.test.ts --reporter=verbose` -- all tests pass
- `npx prisma validate` -- schema is valid
- `grep -n "userId.*String" prisma/schema.prisma` shows `userId String` (no `?`)
- `grep -n "ADMIN_EMAIL" lib/auth/authorization.ts` shows the hardcoded constant
</verification>

<success_criteria>
- Authorization module exports ADMIN_EMAIL, isAdmin, getAuthorizedProject, getAuthorizedProjects
- All unit tests pass (11+ test cases)
- Prisma schema has non-nullable userId on Project
- Migration SQL correctly backfills NULL rows before ALTER
- Prisma client regenerated successfully
</success_criteria>

<output>
After completion, create `.planning/phases/28-data-isolation/28-01-SUMMARY.md`
</output>
