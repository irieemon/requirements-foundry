---
phase: 27-auth-flow
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - app/page.tsx
  - app/layout.tsx
  - app/(authenticated)/layout.tsx
  - app/(authenticated)/projects/page.tsx
  - app/(authenticated)/projects/[id]/page.tsx
  - app/(authenticated)/projects/[id]/export/page.tsx
  - app/(authenticated)/runs/page.tsx
  - app/(authenticated)/runs/[id]/page.tsx
autonomous: false
requirements: [AUTH-02, AUTH-05]

must_haves:
  truths:
    - "Unauthenticated user visiting / sees a clean landing page with app name, tagline, and 'Sign in with Okta' button"
    - "Landing page has no sidebar, no header, no AppShell -- standalone full-page centered login card"
    - "'Sign in with Okta' button links to Cognito authorize URL with identity_provider=Okta"
    - "Authenticated routes (/projects, /runs, etc.) render inside AppShell with sidebar and header"
    - "Root layout no longer wraps everything in AppShell"
  artifacts:
    - path: "app/page.tsx"
      provides: "Public landing page with sign-in button"
      min_lines: 20
    - path: "app/layout.tsx"
      provides: "Root layout without AppShell (just html/body/fonts/toaster)"
    - path: "app/(authenticated)/layout.tsx"
      provides: "Authenticated route group layout with AppShell wrapper"
  key_links:
    - from: "app/page.tsx"
      to: "lib/auth/cognito.ts"
      via: "buildAuthorizeUrl for sign-in button href"
      pattern: "buildAuthorizeUrl"
    - from: "app/(authenticated)/layout.tsx"
      to: "components/layout/app-shell.tsx"
      via: "AppShell wrapper for authenticated pages"
      pattern: "AppShell"
---

<objective>
Create the public landing page and restructure the app layout so that the landing page renders without AppShell while all authenticated routes keep the existing AppShell experience.

Purpose: Users need a clean entry point to sign in (AUTH-02), and the layout must be split so unauthenticated and authenticated routes have different shells (AUTH-05).
Output: Landing page at /, route group (authenticated) with AppShell, existing pages moved under route group.
</objective>

<execution_context>
@/Users/smcine01/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smcine01/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-auth-flow/27-CONTEXT.md
@.planning/phases/27-auth-flow/27-RESEARCH.md
@.planning/phases/27-auth-flow/27-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (lib/auth/) -->
From lib/auth/cognito.ts:
```typescript
export function buildAuthorizeUrl(returnTo?: string): string;
```

From app/layout.tsx (current):
```typescript
// Currently wraps ALL children in AppShell
// Must be changed to NOT include AppShell
import { AppShell } from "@/components/layout/app-shell";
```

From components/layout/app-shell.tsx:
```typescript
// Existing shell component with sidebar and header
// Will be moved to (authenticated) layout
```

Existing page routes to move into (authenticated) group:
- app/projects/page.tsx -> app/(authenticated)/projects/page.tsx
- app/projects/[id]/page.tsx -> app/(authenticated)/projects/[id]/page.tsx
- app/projects/[id]/export/page.tsx -> app/(authenticated)/projects/[id]/export/page.tsx
- app/runs/page.tsx -> app/(authenticated)/runs/page.tsx
- app/runs/[id]/page.tsx -> app/(authenticated)/runs/[id]/page.tsx

API routes stay at app/api/ (NOT inside route group -- route groups only affect layout, not API paths).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure layouts and create landing page</name>
  <files>app/page.tsx, app/layout.tsx, app/(authenticated)/layout.tsx</files>
  <action>
    1. Update `app/layout.tsx` to REMOVE AppShell wrapping:
       - Keep: html, body, fonts, Toaster, skip-link, metadata
       - Remove: AppShell import and wrapper
       - Remove: the `<main>` wrapper (each layout provides its own)
       - This makes the root layout a bare shell -- route-specific layouts add their own chrome

    2. Create `app/(authenticated)/layout.tsx`:
       - Import AppShell from `@/components/layout/app-shell`
       - Wrap children in AppShell and `<main id="main-content" role="main" className="min-h-screen">`
       - This is the layout for all pages that need the sidebar/header
       - Note: (authenticated) is a Next.js route group -- it creates a layout boundary without affecting the URL path. /projects still works, not /(authenticated)/projects.

    3. Replace `app/page.tsx` (currently just `redirect("/projects")`):
       - This is a SERVER COMPONENT that renders the landing page
       - Import `buildAuthorizeUrl` from `@/lib/auth/cognito`
       - Render a full-page centered card with:
         - App name: "Requirements Foundry"
         - Tagline: "Transform use cases into JIRA-ready epics and user stories" (from existing metadata)
         - "Sign in with Okta" button that links to `buildAuthorizeUrl()`
         - Clean, minimal design using existing Tailwind classes and shadcn/ui Card component
       - NO AppShell, NO sidebar, NO header -- standalone full-page design per user decision
       - If `?error=auth_failed` or `?error=no_code` query param present, show a subtle error message
       - If `?returnTo` query param present, pass it to buildAuthorizeUrl() for deep link preservation
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Landing page renders at / with sign-in button. Root layout has no AppShell. (authenticated) layout wraps children in AppShell.</done>
</task>

<task type="auto">
  <name>Task 2: Move existing pages into (authenticated) route group</name>
  <files>app/(authenticated)/projects/page.tsx, app/(authenticated)/projects/[id]/page.tsx, app/(authenticated)/projects/[id]/export/page.tsx, app/(authenticated)/runs/page.tsx, app/(authenticated)/runs/[id]/page.tsx</files>
  <action>
    Move all existing page routes into the (authenticated) route group. This is a file move operation -- the page content does NOT change, only the directory location.

    1. Create directory structure:
       - `app/(authenticated)/projects/`
       - `app/(authenticated)/projects/[id]/`
       - `app/(authenticated)/projects/[id]/export/`
       - `app/(authenticated)/runs/`
       - `app/(authenticated)/runs/[id]/`

    2. Move files (content unchanged):
       - `app/projects/page.tsx` -> `app/(authenticated)/projects/page.tsx`
       - `app/projects/[id]/page.tsx` -> `app/(authenticated)/projects/[id]/page.tsx`
       - `app/projects/[id]/export/page.tsx` -> `app/(authenticated)/projects/[id]/export/page.tsx`
       - `app/runs/page.tsx` -> `app/(authenticated)/runs/page.tsx`
       - `app/runs/[id]/page.tsx` -> `app/(authenticated)/runs/[id]/page.tsx`

    3. Check for any `layout.tsx` files in `app/projects/` or `app/runs/` -- if they exist, move those too.

    4. Remove the now-empty `app/projects/` and `app/runs/` directories.

    5. IMPORTANT: Do NOT move `app/api/` -- API routes stay where they are. Route groups only affect page layouts.

    6. Verify no import paths broke -- the `@/` alias resolves from project root, so component imports inside page files should still work. Only the file location changes, not the content.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>All existing pages render under (authenticated) route group with AppShell. URLs unchanged (/projects, /runs, etc.). Build succeeds with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify landing page and layout split</name>
  <files>app/page.tsx, app/(authenticated)/layout.tsx</files>
  <action>
    Human verification of visual appearance and layout structure.

    1. Run `npm run dev` and visit http://localhost:3000/
    2. Confirm: Landing page shows app name, tagline, and "Sign in with Okta" button
    3. Confirm: No sidebar or header visible on landing page
    4. Confirm: The sign-in button href points to a Cognito authorize URL (inspect with browser dev tools -- it will have identity_provider=Okta in the URL)
    5. Visit http://localhost:3000/?error=auth_failed -- confirm error message appears
    6. Note: Clicking the sign-in button will fail unless Cognito is deployed and Okta SAML is configured. The URL construction is what matters.
    7. If you can access /projects directly (proxy not blocking in dev), confirm it still has the sidebar/header from AppShell

    Resume signal: Type "approved" or describe issues
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>User approves landing page visual appearance and confirms AppShell renders only on authenticated routes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Landing page at / renders sign-in button with Cognito authorize URL
- Authenticated pages at /projects, /runs render inside AppShell
- No sidebar/header visible on landing page
- All existing page functionality preserved (just moved to route group)
</verification>

<success_criteria>
- Clean landing page at / with "Sign in with Okta" button (AUTH-02)
- AppShell only renders for authenticated route group pages
- Existing page URLs unchanged (/projects, /runs still work)
- Build succeeds with restructured layout
- User approves visual appearance of landing page
</success_criteria>

<output>
After completion, create `.planning/phases/27-auth-flow/27-03-SUMMARY.md`
</output>
