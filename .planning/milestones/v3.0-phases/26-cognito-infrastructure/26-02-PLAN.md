---
phase: 26-cognito-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - infra/lib/requirements-foundry-stack.ts
  - infra/test/requirements-foundry-stack.test.ts
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-03]
user_setup:
  - service: okta
    why: "SAML federation requires Okta app configuration by IT team"
    env_vars: []
    dashboard_config:
      - task: "Create SAML 2.0 app in Okta admin console with placeholder ACS URL and Entity ID"
        location: "Okta Admin > Applications > Create App Integration > SAML 2.0"
      - task: "Configure attribute statements: email=user.email, firstName=user.firstName, lastName=user.lastName"
        location: "Okta SAML app > General > SAML Settings > Attribute Statements"
      - task: "Configure group attribute statement: Name=groups, Filter=Matches regex '.*'"
        location: "Okta SAML app > General > SAML Settings > Group Attribute Statements"
      - task: "Get metadata URL from Okta app and pass as CDK context: -c oktaMetadataUrl=https://..."
        location: "Okta SAML app > Sign On tab > Metadata URL"
      - task: "After CDK deploy, update Okta app with real ACS URL and Entity ID from CDK outputs"
        location: "Okta SAML app > General > SAML Settings > Single Sign On URL and Audience URI"

must_haves:
  truths:
    - "Cognito User Pool is deployed with selfSignUpEnabled=false and email sign-in"
    - "Cognito domain exists for Hosted UI access"
    - "Okta SAML identity provider is configured on the User Pool with metadata URL from context"
    - "User Pool Client has authorization code grant flow with openid/email/profile scopes"
    - "PreTokenGeneration Lambda trigger is wired to the User Pool with V2_0 version"
    - "Cognito client secret is extracted via AwsCustomResource and stored in Secrets Manager"
    - "ECS container receives Cognito credentials via environment vars and secrets"
    - "CDK outputs include Entity ID, ACS URL, and Hosted UI URL for Okta configuration"
  artifacts:
    - path: "infra/lib/requirements-foundry-stack.ts"
      provides: "Cognito UserPool, SAML IdP, domain, client, Lambda trigger, secret extraction, ECS wiring"
      contains: "UserPool"
    - path: "infra/test/requirements-foundry-stack.test.ts"
      provides: "CDK assertion tests for all Cognito resources"
      contains: "Cognito"
  key_links:
    - from: "infra/lib/requirements-foundry-stack.ts"
      to: "infra/lambda/pre-token-generation/index.ts"
      via: "lambda.Code.fromAsset('lambda/pre-token-generation')"
      pattern: "fromAsset.*pre-token-generation"
    - from: "infra/lib/requirements-foundry-stack.ts (AwsCustomResource)"
      to: "infra/lib/requirements-foundry-stack.ts (Secrets Manager)"
      via: "describeUserPoolClient response -> secretObjectValue"
      pattern: "UserPoolClient\\.ClientSecret"
    - from: "infra/lib/requirements-foundry-stack.ts (cognitoSecret)"
      to: "infra/lib/requirements-foundry-stack.ts (ECS container)"
      via: "ecs.Secret.fromSecretsManager(cognitoSecret)"
      pattern: "COGNITO_CLIENT_SECRET"
    - from: "infra/lib/requirements-foundry-stack.ts (cognitoSecret)"
      to: "infra/lib/requirements-foundry-stack.ts (taskExecutionRole)"
      via: "cognitoSecret.grantRead(taskExecutionRole)"
      pattern: "grantRead.*taskExecutionRole"
---

<objective>
Add Cognito User Pool with Okta SAML federation, PreTokenGeneration Lambda trigger, client secret extraction, and ECS credential injection to the existing CDK stack.

Purpose: This is the core infrastructure plan for Phase 26. It deploys all AWS resources needed for Cognito-based authentication and wires credentials to the ECS application container so Phase 27 can implement the auth flow.
Output: Updated CDK stack with Cognito resources, passing CDK assertion tests, CDK outputs for Okta configuration.
</objective>

<execution_context>
@/Users/smcine01/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smcine01/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-cognito-infrastructure/26-RESEARCH.md
@.planning/phases/26-cognito-infrastructure/26-01-SUMMARY.md

@infra/lib/requirements-foundry-stack.ts
@infra/test/requirements-foundry-stack.test.ts

<interfaces>
<!-- Existing stack constructs the executor needs to wire into -->

From infra/lib/requirements-foundry-stack.ts:
```typescript
// Line 175: ALB (need loadBalancerDnsName for callback URLs)
const alb = new elbv2.ApplicationLoadBalancer(this, 'Alb', { ... });

// Line 213: Task execution role (need to grant cognitoSecret read)
const taskExecutionRole = new iam.Role(this, 'TaskExecutionRole', { ... });

// Line 265: Cron secret (pattern to follow for new secret)
const cronSecret = new secretsmanager.Secret(this, 'CronSecret', { ... });
// Line 272: Pattern for granting read to execution role
cronSecret.grantRead(taskExecutionRole);

// Line 275: Container definition (need to add Cognito env vars and secrets)
taskDefinition.addContainer('AppContainer', {
  environment: { NODE_ENV, PORT, AWS_REGION, S3_BUCKET_NAME, RDS_SECRET_NAME },
  secrets: { CRON_SECRET: ecs.Secret.fromSecretsManager(cronSecret) },
  ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Cognito resources to CDK stack with CDK assertion tests</name>
  <files>infra/lib/requirements-foundry-stack.ts, infra/test/requirements-foundry-stack.test.ts</files>
  <behavior>
    - Test: Stack creates a Cognito UserPool with selfSignUpEnabled=false
    - Test: Stack creates a Cognito UserPool domain
    - Test: Stack creates a SAML identity provider named 'Okta'
    - Test: Stack creates a UserPoolClient with authorization code grant
    - Test: Stack creates a PreTokenGeneration Lambda function
    - Test: UserPool has a Lambda trigger configured
    - Test: Stack creates an AwsCustomResource for describing the UserPool client
    - Test: Stack creates a Secrets Manager secret for Cognito client credentials
    - Test: Task execution role has permission to read the Cognito secret
    - Test: ECS container definition includes COGNITO_USER_POOL_ID and COGNITO_CLIENT_ID environment variables
    - Test: ECS container definition includes COGNITO_CLIENT_SECRET in secrets
    - Test: Stack outputs include CognitoEntityId, CognitoAcsUrl, CognitoHostedUiUrl, CognitoClientId
    - Test: UserPool has custom:groups attribute defined
  </behavior>
  <action>
    **Write CDK assertion tests first** in `infra/test/requirements-foundry-stack.test.ts`:
    Add a new `describe('Cognito Infrastructure', () => { ... })` block after existing tests. Write assertions using `Template` and `Match` from `aws-cdk-lib/assertions` (already imported). Tests should assert:
    - `AWS::Cognito::UserPool` exists with `Policies.PasswordPolicy` (selfSignUp is not a CloudFormation property -- assert pool exists)
    - `AWS::Cognito::UserPoolDomain` exists
    - `AWS::Cognito::UserPoolIdentityProvider` exists with `ProviderType: 'SAML'` and `ProviderName: 'Okta'`
    - `AWS::Cognito::UserPoolClient` exists with `AllowedOAuthFlows: ['code']` and `AllowedOAuthScopes` including 'openid'
    - `AWS::Lambda::Function` exists with `FunctionName: 'requirements-foundry-pre-token-generation'`
    - `AWS::CloudFormation::CustomResource` or `Custom::DescribeCognitoUserPoolClient` exists (for AwsCustomResource)
    - `AWS::SecretsManager::Secret` with `Name: 'requirements-foundry-prod/cognito-client'` exists
    - Stack outputs include `CognitoEntityId`, `CognitoAcsUrl`, `CognitoHostedUiUrl`, `CognitoClientId`
    - Container definition includes `COGNITO_USER_POOL_ID` in environment
    - Update the existing "at least 16 outputs" test to "at least 21 outputs" (adding 5 Cognito outputs)
    - Update the existing "at least 2 secrets" test to "at least 3 secrets" (adding Cognito secret)

    Also update the test `beforeAll` to pass CDK context for the Okta metadata URL so the stack synthesizes without error:
    ```typescript
    const app = new cdk.App({
      context: {
        oktaMetadataUrl: 'https://test.okta.com/app/test123/sso/saml/metadata',
      },
    });
    ```

    **Then implement in** `infra/lib/requirements-foundry-stack.ts`:

    Add imports at top:
    ```typescript
    import * as cognito from 'aws-cdk-lib/aws-cognito';
    import * as cr from 'aws-cdk-lib/custom-resources';
    ```

    Add Cognito resources after the CloudWatch alarms section (before Stack Outputs), in this order:

    1. **UserPool** with `selfSignUpEnabled: false`, `signInAliases: { email: true }`, standard attributes (email required, givenName/familyName optional), `customAttributes: { groups: new cognito.StringAttribute({ mutable: true }) }`, `removalPolicy: DESTROY`.

    2. **Cognito Domain** using `userPool.addDomain()` with `cognitoDomain.domainPrefix` from `this.node.tryGetContext('cognitoDomainPrefix') || 'requirements-foundry-prod'`.

    3. **Okta SAML IdP** using `new cognito.UserPoolIdentityProviderSaml()` with metadata URL from `this.node.tryGetContext('oktaMetadataUrl')` (fallback to placeholder URL). Attribute mapping: email -> `http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress`, custom groups -> `groups`.

    4. **PreTokenGeneration Lambda** using `new lambda.Function()` with `runtime: NODEJS_20_X`, `handler: 'index.handler'`, `code: lambda.Code.fromAsset('lambda/pre-token-generation')`, timeout 5s, memorySize 128. Then `userPool.addTrigger(cognito.UserPoolOperation.PRE_TOKEN_GENERATION_CONFIG, preTokenFn, cognito.LambdaVersion.V2_0)`.

    5. **UserPoolClient** using `userPool.addClient()` with `generateSecret: true`, OAuth flows `authorizationCodeGrant: true`, scopes `OPENID, EMAIL, PROFILE`, callbackUrls `[http://${alb.loadBalancerDnsName}/api/auth/callback]`, logoutUrls `[http://${alb.loadBalancerDnsName}/]`, `supportedIdentityProviders: [cognito.UserPoolClientIdentityProvider.custom('Okta')]`, token validity (1h access, 1h id, 30d refresh). Add `client.node.addDependency(samlProvider)`.

    6. **AwsCustomResource** to call `describeUserPoolClient` and extract client secret (follow Pattern 2 from RESEARCH.md exactly). Use `onCreate` and `onUpdate` handlers.

    7. **Secrets Manager Secret** for Cognito client credentials with `secretObjectValue` containing userPoolId, clientId, clientSecret (from AwsCustomResource), and domain.

    8. **Grant read** on cognitoSecret to `taskExecutionRole`: `cognitoSecret.grantRead(taskExecutionRole)`.

    9. **Update existing `taskDefinition.addContainer('AppContainer', {...})`** to add:
       - `environment`: add `COGNITO_USER_POOL_ID`, `COGNITO_CLIENT_ID`, `COGNITO_DOMAIN`, `COGNITO_REDIRECT_URI`
       - `secrets`: add `COGNITO_CLIENT_SECRET: ecs.Secret.fromSecretsManager(cognitoSecret)`
       Note: The container definition is created inline (line 275). The Cognito resources must be defined BEFORE the container definition in the file, OR the variables must be hoisted. Simplest: move the Cognito section to BEFORE the ECS container definition (after taskDefinition, before addContainer).

    10. **CDK Outputs** (add after existing outputs):
        - `CognitoUserPoolId` with export `rf-prod-cognito-user-pool-id`
        - `CognitoEntityId` with value `urn:amazon:cognito:sp:${userPool.userPoolId}` and description for Okta
        - `CognitoAcsUrl` with value `https://${domain.domainName}.auth.us-east-1.amazoncognito.com/saml2/idpresponse`
        - `CognitoHostedUiUrl` using `domain.signInUrl(client, { redirectUri })` with description for testing
        - `CognitoClientId` with export `rf-prod-cognito-client-id`

    IMPORTANT ordering note: The `alb` variable is defined at line 175. The `taskExecutionRole` at line 213. The container `addContainer` at line 275. Cognito resources should be added between the cronSecret.grantRead(taskExecutionRole) block (line 272) and the addContainer call (line 275), so that cognitoSecret is available when defining the container. This means inserting the Cognito block at approximately line 273.

    Do NOT use `cdk.SecretValue.unsafePlainText()` for the domain -- it is not truly secret and this is acceptable per the research pattern. For clientSecret from AwsCustomResource, use `cdk.SecretValue.unsafePlainText(describeCognitoClient.getResponseField(...))` as shown in research Pattern 2.
  </action>
  <verify>
    <automated>cd infra && npx jest</automated>
  </verify>
  <done>All existing tests still pass. New Cognito tests pass. Stack synthesizes with test context. UserPool, domain, SAML IdP, client, Lambda trigger, AwsCustomResource, Cognito secret, ECS wiring, and 5 new outputs are all verified by CDK assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CDK stack synthesizes correctly with Cognito resources</name>
  <files>infra/lib/requirements-foundry-stack.ts</files>
  <action>
    Human verifies the complete Cognito infrastructure in CDK. Automated tests already passed in Task 1. This checkpoint confirms the synthesized CloudFormation template looks correct before any deployment.

    Steps to verify:
    1. Run `cd infra && npx cdk synth --context oktaMetadataUrl=https://placeholder.okta.com/app/test/sso/saml/metadata 2>&1 | head -20` to verify stack synthesizes without errors
    2. Run `cd infra && npx cdk synth --context oktaMetadataUrl=https://placeholder.okta.com/app/test/sso/saml/metadata | grep -c "AWS::Cognito"` to verify Cognito resources appear in template (expect 4+: UserPool, Domain, IdP, Client)
    3. Run `cd infra && npx cdk synth --context oktaMetadataUrl=https://placeholder.okta.com/app/test/sso/saml/metadata | grep "CognitoEntityId\|CognitoAcsUrl\|CognitoHostedUiUrl"` to verify outputs exist
    4. Review the Cognito section in `infra/lib/requirements-foundry-stack.ts` for correctness
    5. Confirm you are comfortable with the CDK changes before deploying
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User has reviewed and approved the CDK stack changes for Cognito infrastructure</done>
</task>

</tasks>

<verification>
cd infra && npx jest
All tests pass (existing + new Cognito tests).

cd infra && npx cdk synth --context oktaMetadataUrl=https://placeholder.okta.com/app/test/sso/saml/metadata > /dev/null 2>&1 && echo "Synth OK"
Stack synthesizes successfully.
</verification>

<success_criteria>
- Cognito UserPool exists in stack with selfSignUpEnabled=false and custom:groups attribute
- Cognito domain is configured for Hosted UI
- Okta SAML IdP is attached to UserPool with metadata URL from CDK context
- UserPoolClient has auth code grant with openid/email/profile scopes
- PreTokenGeneration Lambda is wired as V2_0 trigger
- AwsCustomResource extracts client secret and stores in Secrets Manager
- ECS container has COGNITO_USER_POOL_ID, COGNITO_CLIENT_ID, COGNITO_DOMAIN, COGNITO_REDIRECT_URI in environment
- ECS container has COGNITO_CLIENT_SECRET from Secrets Manager in secrets
- Task execution role can read Cognito secret
- CDK outputs include Entity ID, ACS URL, Hosted UI URL for Okta configuration
- All CDK assertion tests pass
- User approves stack changes
</success_criteria>

<output>
After completion, create `.planning/phases/26-cognito-infrastructure/26-02-SUMMARY.md`
</output>
