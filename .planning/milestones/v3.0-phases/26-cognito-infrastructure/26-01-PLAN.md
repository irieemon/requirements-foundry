---
phase: 26-cognito-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/lambda/pre-token-generation/index.ts
  - infra/test/pre-token-generation.test.ts
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "PreTokenGeneration Lambda correctly parses Okta groups from custom:groups attribute"
    - "Lambda handles JSON array format for groups"
    - "Lambda handles comma-separated string format for groups"
    - "Lambda merges Okta groups with existing Cognito groups without duplicates"
    - "Lambda returns valid V2_0 PreTokenGeneration response structure"
  artifacts:
    - path: "infra/lambda/pre-token-generation/index.ts"
      provides: "PreTokenGeneration Lambda handler"
      exports: ["handler"]
      min_lines: 25
    - path: "infra/test/pre-token-generation.test.ts"
      provides: "Unit tests for Lambda handler"
      min_lines: 40
  key_links:
    - from: "infra/lambda/pre-token-generation/index.ts"
      to: "Cognito PreTokenGeneration V2_0 event"
      via: "event.request.userAttributes['custom:groups'] -> event.response.claimsAndScopeOverrideDetails.groupOverrideDetails.groupsToOverride"
      pattern: "groupsToOverride"
---

<objective>
Create the PreTokenGeneration Lambda handler that maps Okta SAML group attributes to cognito:groups in JWT tokens, with comprehensive unit tests.

Purpose: This Lambda is the only custom code in Phase 26. It bridges Okta's group assertion (stored in custom:groups attribute) to Cognito's native group system, enabling downstream phases to use cognito:groups for authorization.
Output: Lambda handler file and passing unit tests.
</objective>

<execution_context>
@/Users/smcine01/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smcine01/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-cognito-infrastructure/26-RESEARCH.md

@infra/tsconfig.json
@infra/jest.config.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PreTokenGeneration Lambda handler and unit tests</name>
  <files>infra/lambda/pre-token-generation/index.ts, infra/test/pre-token-generation.test.ts</files>
  <behavior>
    - Test: Handler returns unmodified event when custom:groups is missing/empty
    - Test: Handler parses JSON array groups (e.g., '["admin","users"]') into groupsToOverride
    - Test: Handler parses comma-separated groups (e.g., "admin, users") into groupsToOverride
    - Test: Handler parses single group string (e.g., "admin") into groupsToOverride
    - Test: Handler merges Okta groups with existing Cognito groups without duplicates
    - Test: Handler filters out empty strings from parsed groups
    - Test: Handler returns correct V2_0 response structure with claimsAndScopeOverrideDetails
  </behavior>
  <action>
    Create `infra/lambda/pre-token-generation/` directory.

    Write `infra/lambda/pre-token-generation/index.ts`:
    - Export `handler` async function accepting PreTokenGenerationV2Event
    - Define TypeScript interface for the V2_0 PreTokenGeneration event (request.userAttributes, request.groupConfiguration with groupsToOverride/iamRolesToOverride/preferredRole, response with claimsAndScopeOverrideDetails.groupOverrideDetails)
    - Read `event.request.userAttributes['custom:groups']`
    - Try JSON.parse first -- if result is array, use it
    - Fallback: split on comma, trim each, filter empty
    - Merge with `event.request.groupConfiguration.groupsToOverride || []` using Set for dedup
    - Set `event.response.claimsAndScopeOverrideDetails.groupOverrideDetails.groupsToOverride = allGroups`
    - Return event
    - Add console.log for raw custom:groups value (helps debugging per research pitfall 3)

    Write `infra/test/pre-token-generation.test.ts`:
    - Import handler from `../lambda/pre-token-generation/index`
    - Create helper function `makeEvent(overrides)` that builds a valid V2_0 event object
    - Write tests for each behavior listed above
    - Note: Jest config roots is `['<rootDir>/test']` so this file is auto-discovered
    - Note: tsconfig uses `module: "NodeNext"` and `moduleResolution: "NodeNext"` -- import must match (use `.js` extension in import if needed, or rely on ts-jest transform)
  </action>
  <verify>
    <automated>cd infra && npx jest --testPathPattern pre-token-generation</automated>
  </verify>
  <done>All 7+ unit tests pass. Lambda handler correctly maps Okta groups in JSON array, comma-separated, and single-value formats. Empty/missing groups handled gracefully.</done>
</task>

</tasks>

<verification>
cd infra && npx jest --testPathPattern pre-token-generation
All tests pass with 0 failures.
</verification>

<success_criteria>
- infra/lambda/pre-token-generation/index.ts exports a handler function
- infra/test/pre-token-generation.test.ts has 7+ passing tests
- Handler handles JSON array, comma-separated, single value, empty, and missing group formats
- Handler merges with existing Cognito groups without duplicates
- Handler returns valid V2_0 response structure
</success_criteria>

<output>
After completion, create `.planning/phases/26-cognito-infrastructure/26-01-SUMMARY.md`
</output>
