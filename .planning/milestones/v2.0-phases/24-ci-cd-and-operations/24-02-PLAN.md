---
phase: 24-ci-cd-and-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - CICD-01

must_haves:
  truths:
    - "A push to main triggers a GitHub Actions workflow that builds a Docker image, pushes to ECR, and deploys to ECS"
    - "The workflow uses OIDC authentication (id-token: write permission) with no long-lived AWS credentials"
    - "Docker build targets linux/arm64 to match the ECS task definition ARM64 runtime platform"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Automated deployment pipeline"
      contains: "aws-actions/configure-aws-credentials"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "ECR repository"
      via: "docker push"
      pattern: "docker push.*ECR_REGISTRY"
    - from: ".github/workflows/deploy.yml"
      to: "ECS service"
      via: "aws ecs update-service --force-new-deployment"
      pattern: "update-service.*force-new-deployment"
---

<objective>
Create the GitHub Actions deployment workflow that automates the build-push-deploy cycle on push to main.

Purpose: Eliminates manual deploy.sh runs. Every merge to main automatically builds the Docker image, pushes to ECR, and triggers an ECS rolling deployment via OIDC-authenticated AWS credentials.
Output: `.github/workflows/deploy.yml` ready to use once OIDC infrastructure (Plan 01) is deployed.
</objective>

<execution_context>
@/Users/smcine01/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smcine01/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ci-cd-and-operations/24-CONTEXT.md
@.planning/phases/24-ci-cd-and-operations/24-RESEARCH.md

@scripts/deploy.sh

<interfaces>
<!-- From deploy.sh — the manual workflow being automated -->
AWS_REGION: us-east-1
ECR_REPOSITORY: requirements-foundry-prod
ECS_CLUSTER: requirements-foundry-prod-cluster
ECS_SERVICE: requirements-foundry-prod-service

<!-- From CDK stack (Plan 01 creates the role) -->
IAM Role name: requirements-foundry-github-actions
Role ARN pattern: arn:aws:iam::<ACCOUNT_ID>:role/requirements-foundry-github-actions

<!-- From existing CDK task definition -->
Runtime platform: ARM64 (ecs.CpuArchitecture.ARM64)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Create directory `.github/workflows/` if it does not exist.

Create `.github/workflows/deploy.yml` with the following structure:

```yaml
name: Deploy to ECS

on:
  push:
    branches: [main]

permissions:
  id-token: write   # Required for OIDC
  contents: read     # Required for checkout

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: requirements-foundry-prod
  ECS_CLUSTER: requirements-foundry-prod-cluster
  ECS_SERVICE: requirements-foundry-prod-service

jobs:
  deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/requirements-foundry-github-actions
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image
        env:
          ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build --platform linux/arm64 -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:latest
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

      - name: Deploy to ECS
        run: |
          aws ecs update-service \
            --cluster $ECS_CLUSTER \
            --service $ECS_SERVICE \
            --force-new-deployment \
            --region $AWS_REGION
```

Key decisions per user constraints:
- Push to `main` triggers — no PR checks, no manual approval gates
- Pipeline is build/push/deploy only — no lint/typecheck/test steps
- Workflow steps inline (not calling deploy.sh) — deploy.sh remains for manual deploys
- Use `--platform linux/arm64` because the ECS task definition uses ARM64 runtime platform
- Use `aws ecs update-service --force-new-deployment` (NOT `amazon-ecs-deploy-task-definition` action) because task definition is CDK-managed
- `AWS_ACCOUNT_ID` stored as a GitHub repository secret (needed to construct the IAM role ARN)

Do NOT include:
- PR checks or test steps (deferred per user decision)
- CDK deployment steps (manual per user decision)
- Environment approval gates
  </action>
  <verify>
    <automated>cat "/Users/smcine01/Library/CloudStorage/OneDrive-dentsu/Documents/Merkle/Claude Projects/Requirements Foundy/.github/workflows/deploy.yml" | grep -c "configure-aws-credentials\|amazon-ecr-login\|force-new-deployment\|id-token: write\|linux/arm64"</automated>
  </verify>
  <done>deploy.yml exists with 5 key markers: OIDC credential config, ECR login, force-new-deployment, id-token permission, and ARM64 platform target. File uses `secrets.AWS_ACCOUNT_ID` for the role ARN.</done>
</task>

<task type="auto">
  <name>Task 2: Add .gitignore entry and validate workflow syntax</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Validate the workflow YAML is syntactically correct by checking:
1. The file parses as valid YAML (use a quick Node.js or Python YAML parse check)
2. Required fields present: `on`, `permissions`, `jobs`, `steps`
3. The `role-to-assume` field uses `${{ secrets.AWS_ACCOUNT_ID }}` interpolation correctly

Also verify the `.github/` directory is not in `.gitignore` — it must be committed.

If `.gitignore` contains `.github` or `.github/`, remove that entry.

Print a summary of the workflow structure to confirm correctness.
  </action>
  <verify>
    <automated>node -e "const yaml = require('yaml'); const fs = require('fs'); const doc = yaml.parse(fs.readFileSync('/Users/smcine01/Library/CloudStorage/OneDrive-dentsu/Documents/Merkle/Claude Projects/Requirements Foundy/.github/workflows/deploy.yml', 'utf8')); console.log('Valid YAML. Jobs:', Object.keys(doc.jobs)); console.log('Trigger:', JSON.stringify(doc.on)); console.log('Permissions:', JSON.stringify(doc.permissions));" 2>&1 || echo "YAML parse failed"</automated>
  </verify>
  <done>Workflow YAML is valid, parses correctly, and has the expected structure: push-to-main trigger, OIDC permissions, single deploy job.</done>
</task>

</tasks>

<verification>
- `.github/workflows/deploy.yml` exists and is valid YAML
- Workflow triggers on push to `main` only
- Permissions include `id-token: write` for OIDC
- Docker build uses `--platform linux/arm64`
- Deploys via `aws ecs update-service --force-new-deployment`
- No lint, test, or CDK deploy steps present
</verification>

<success_criteria>
- GitHub Actions workflow file exists at `.github/workflows/deploy.yml`
- Workflow automates the complete build/push/deploy cycle matching deploy.sh
- OIDC authentication configured (no long-lived AWS credentials)
- ARM64 platform specified for Docker build (matches ECS task definition)
- Ready to execute once OIDC IAM role is deployed (Plan 01) and AWS_ACCOUNT_ID secret is set in GitHub
</success_criteria>

<output>
After completion, create `.planning/phases/24-ci-cd-and-operations/24-02-SUMMARY.md`
</output>
