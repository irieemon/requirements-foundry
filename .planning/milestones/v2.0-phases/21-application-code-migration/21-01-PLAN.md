---
phase: 21-application-code-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - Dockerfile
  - .dockerignore
  - next.config.ts
  - lib/db.ts
  - app/api/health/route.ts
autonomous: true
requirements:
  - CODE-03
  - CODE-04
  - CODE-06
  - CODE-08

must_haves:
  truths:
    - "npm install succeeds with AWS SDKs added and Vercel/Anthropic packages removed"
    - "docker build produces a working image under 500MB"
    - "docker run starts the app on port 3000 in MOCK_MODE"
    - "Health check endpoint returns 200 with AWS-oriented fields"
    - "Database connection uses standard connection string without Vercel SSL detection"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage standalone Next.js container image"
      min_lines: 25
    - path: ".dockerignore"
      provides: "Docker build context exclusions"
      min_lines: 5
    - path: "next.config.ts"
      provides: "Standalone output mode for Docker"
      contains: "output.*standalone"
    - path: "lib/db.ts"
      provides: "Simplified database connection"
    - path: "app/api/health/route.ts"
      provides: "AWS-oriented health check"
  key_links:
    - from: "Dockerfile"
      to: "next.config.ts"
      via: "standalone output mode produces .next/standalone/server.js"
      pattern: "output.*standalone"
    - from: "lib/db.ts"
      to: "process.env.DATABASE_URL"
      via: "standard connection string, no Vercel detection"
      pattern: "connectionString"
---

<objective>
Set up the foundation for AWS migration: swap package dependencies, create the Dockerfile, simplify database connection, update health check, and enable standalone mode in next.config.ts.

Purpose: Establishes the build and runtime foundation that all other plans depend on. Package changes must land first since they affect imports in Plans 02 and 03.
Output: Working Docker build, updated packages, simplified db.ts and health check.
</objective>

<execution_context>
@/Users/smcine01/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smcine01/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-application-code-migration/21-CONTEXT.md
@.planning/phases/21-application-code-migration/21-RESEARCH.md

<interfaces>
<!-- Current interfaces the executor needs to understand -->

From lib/db.ts (REWRITE target):
```typescript
// Current: Vercel/Neon SSL detection logic to REMOVE
const isVercel = connectionString.includes("vercel-storage.com") ||
  connectionString.includes("neon.tech") ||
  process.env.VERCEL === "1";
const adapter = new PrismaPg({ connectionString, ssl: isVercel ? { rejectUnauthorized: false } : undefined });
```

From app/api/health/route.ts (MODIFY target):
```typescript
// Current: Vercel-specific fields to REPLACE
aiEnabled: !!process.env.ANTHROPIC_API_KEY,
environment: process.env.VERCEL_ENV || process.env.NODE_ENV || "development",
region: process.env.VERCEL_REGION || "local",
```

From next.config.ts (MODIFY target):
```typescript
const nextConfig: NextConfig = {
  experimental: { serverActions: { bodySizeLimit: "4mb" } },
  serverExternalPackages: ["@prisma/client"],
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package dependencies and next.config.ts standalone mode</name>
  <files>package.json, package-lock.json, next.config.ts</files>
  <action>
1. Run: `npm install @aws-sdk/client-s3 @aws-sdk/credential-providers @anthropic-ai/bedrock-sdk`
2. Run: `npm uninstall @vercel/blob @anthropic-ai/sdk`
3. Update `next.config.ts`:
   - Add `output: "standalone"` to the nextConfig object
   - Remove the comment about "Vercel Pro: up to 300s timeout for server actions"
   - Remove the comment about "Set max duration for all serverless functions (Vercel Pro: 300s max)"
   - Keep `serverExternalPackages: ["@prisma/client"]` and `experimental.serverActions.bodySizeLimit`
4. Verify `package.json` no longer lists `@vercel/blob` or `@anthropic-ai/sdk` in dependencies
5. Verify `package.json` now lists `@aws-sdk/client-s3`, `@aws-sdk/credential-providers`, `@anthropic-ai/bedrock-sdk`
  </action>
  <verify>
    <automated>cd "/Users/smcine01/Library/CloudStorage/OneDrive-dentsu/Documents/Merkle/Claude Projects/Requirements Foundy" && node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies, ...pkg.devDependencies}; console.assert(!deps['@vercel/blob'], 'vercel/blob still present'); console.assert(!deps['@anthropic-ai/sdk'], 'anthropic/sdk still present'); console.assert(deps['@aws-sdk/client-s3'], 'missing s3'); console.assert(deps['@anthropic-ai/bedrock-sdk'], 'missing bedrock'); console.log('Package deps OK');" && grep -q "standalone" next.config.ts && echo "Standalone OK"</automated>
  </verify>
  <done>AWS SDK packages installed, Vercel/Anthropic packages removed, next.config.ts has output: "standalone" and no Vercel comments</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile, .dockerignore, db.ts simplification, and health check</name>
  <files>Dockerfile, .dockerignore, lib/db.ts, app/api/health/route.ts</files>
  <action>
1. Create `Dockerfile` at project root with multi-stage build:
   - Stage 1 (deps): `node:22-alpine`, copy package*.json, run `npm ci`
   - Stage 2 (builder): `node:22-alpine`, copy all source, run `npx prisma generate && npm run build`
   - Stage 3 (runner): `node:22-alpine`, create non-root user (nodejs/nextjs), copy `.next/standalone`, `.next/static`, `public`, `prisma/` directory, and `node_modules/.prisma`
   - Set `ENV NODE_ENV=production`, `ENV PORT=3000`, `EXPOSE 3000`, `CMD ["node", "server.js"]`
   - Add Prisma binary target hint: set `ENV PRISMA_CLI_QUERY_ENGINE_TYPE=binary` in builder stage
   - Use `USER nextjs` before CMD

2. Create `.dockerignore`:
   ```
   node_modules
   .next
   .git
   .env*
   .planning
   *.md
   .claude
   ```

3. Simplify `lib/db.ts`:
   - Remove the `isVercel` detection block (lines checking vercel-storage.com, neon.tech, process.env.VERCEL)
   - Remove the `ssl` parameter from `new PrismaPg()`
   - Simplify to just: `const adapter = new PrismaPg({ connectionString });`
   - Remove `POSTGRES_URL` fallback -- use only `DATABASE_URL`
   - Update error message: "DATABASE_URL environment variable is required"
   - Remove comment "Vercel Postgres or any PostgreSQL database"
   - Keep the globalForPrisma singleton pattern, keep dev logging config

4. Update `app/api/health/route.ts`:
   - Change `aiEnabled` from `!!process.env.ANTHROPIC_API_KEY` to `process.env.MOCK_MODE !== "true"` (real check happens at provider level)
   - Change `environment` from `process.env.VERCEL_ENV || ...` to `process.env.NODE_ENV || "development"`
   - Change `region` from `process.env.VERCEL_REGION || "local"` to `process.env.AWS_REGION || "local"`
   - Keep the run health status check (DB query) as-is
  </action>
  <verify>
    <automated>cd "/Users/smcine01/Library/CloudStorage/OneDrive-dentsu/Documents/Merkle/Claude Projects/Requirements Foundy" && test -f Dockerfile && echo "Dockerfile exists" && test -f .dockerignore && echo "Dockerignore exists" && ! grep -q "VERCEL" lib/db.ts && echo "db.ts clean" && ! grep -q "VERCEL" app/api/health/route.ts && echo "Health clean" && ! grep -q "ANTHROPIC_API_KEY" app/api/health/route.ts && echo "Health no anthropic key" && ! grep -q "POSTGRES_URL" lib/db.ts && echo "No POSTGRES_URL"</automated>
  </verify>
  <done>Dockerfile builds a standalone Next.js image. db.ts uses standard connection string without Vercel SSL logic. Health check returns AWS-oriented fields with no Vercel references.</done>
</task>

</tasks>

<verification>
1. `npm install` completes without errors
2. `grep -r "VERCEL" lib/db.ts app/api/health/route.ts` returns empty
3. `grep "standalone" next.config.ts` finds the output setting
4. Dockerfile and .dockerignore exist at project root
5. `npx tsc --noEmit` passes (no type errors from package removal)
</verification>

<success_criteria>
- AWS SDK packages in package.json, Vercel packages removed
- next.config.ts has output: "standalone"
- Dockerfile exists with multi-stage build targeting node:22-alpine
- lib/db.ts simplified to standard PrismaPg without SSL detection
- Health check uses NODE_ENV and AWS_REGION instead of Vercel env vars
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/21-application-code-migration/21-01-SUMMARY.md`
</output>
